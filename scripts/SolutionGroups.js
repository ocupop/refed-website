import React from 'react'
import PropTypes from 'prop-types'

const SolutionGroups = ({ capital_type = "Venture Capital" }) => {
  return (
    <div>

    </div>
  )
}

SolutionGroups.propTypes = {

}

export default SolutionGroups
